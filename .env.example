# Node.js executable path
# Run `which node` in your terminal to find your path
NODE_PATH=/usr/local/bin/node

# Upload 快捷键
# Format: modifiers+key (e.g. cmd+alt+X)
# Default: cmd+alt+X
UPLOAD_SHORTCUT=cmd+alt+X
